{
  "name": "agent-governance-assessment",
  "version": "0.3.0",
  "description": "Assessment + configuration + PM app for AI/agent governance in Microsoft 365 — zero runtime dependencies",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=18" }
}
